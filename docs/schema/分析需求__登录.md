# 数据模型文档



## 2. SCHEMA (数据驱动)

### 用户表

```sql
CREATE TABLE users (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email           VARCHAR(255) UNIQUE NOT NULL,
  username        VARCHAR(50) UNIQUE,
  password_hash   VARCHAR(255) NOT NULL,
  status          VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive', 'locked')),
  failed_attempts INTEGER DEFAULT 0,
  locked_until    TIMESTAMP,
  last_login_at   TIMESTAMP,
  created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_status ON users(status);
```

### 登录日志表

```sql
CREATE TABLE login_logs (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id      UUID REFERENCES users(id),
  ip_address   VARCHAR(45),
  user_agent   TEXT,
  status       VARCHAR(20) CHECK (status IN ('success', 'failed')),
  created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_login_logs_user ON login_logs(user_id, created_at DESC);
```

---

   - 验收标准：迁移可执行，表结构正确

2. [Backend] 实现密码哈希工具类（预计 1h）
   - 输入：密码明文
   - 输出：bcrypt 哈希值
   - 依赖：无
   - 验收标准：哈希可验证，强度因子 >= 10

3. [Backend] 实现 JWT Token 服务（预计 2h）
   - 输入：用户 ID, 密钥配置
   - 输出：access_token, refresh_token
   - 依赖：任务 1
   - 验收标准：Token 可签发、可验证、可刷新

4. [Backend] 实现登录 API 核心逻辑（预计 2h）
   - 输入：email, password
   - 输出：用户信息 + Token 或错误响应
   - 依赖：任务 1, 2, 3
   - 验收标准：符合 API 契约，错误码正确

5. [Backend] 实现账户锁定策略（预计 1.5h）
   - 输入：失败次数阈值(3), 锁定时长(15min)
   - 输出：锁定状态更新
   - 依赖：任务 4
   - 验收标准：连续失败 3 次后锁定

6. [Backend] 实现登出 API（预计 0.5h）
   - 输入：refresh_token
   - 输出：成功响应
   - 依赖：任务 3
   - 验收标准：Token 失效

7. [Backend] 实现登录日志记录（预计 1h）
   - 输入：登录事件
   - 输出：日志记录
   - 依赖：任务 1
   - 验收标准：每次登录有记录
```

### Frontend 任务

```
1. [Frontend] 实现登录表单 UI 组件（预计 2h）
   - 输入：设计稿/原型
   - 输出：Login 组件
   - 依赖：无
   - 验收标准：包含邮箱、密码输入，表单验证

2. [Frontend] 实现认证状态管理 Store（预计 1.5h）
   - 输入：用户数据结构
   - 输出：Auth Store (user, token, isAuthenticated)
   - 依赖：无
   - 验收标准：状态可读写，持久化到 localStorage

3. [Frontend] 实现登录 API 调用与错误处理（预计 1.5h）
   - 输入：API 契约
   - 输出：login service, error handling
   - 依赖：Frontend 任务 1, Backend 任务 4 (Sync: API_READY)
   - 验收标准：调用成功后更新 Store

4. [Frontend] 实现路由守卫（预计 1h）
   - 输入：路由配置
   - 输出：Protected Route 组件
   - 依赖：Frontend 任务 2
   - 验收标准：未认证用户重定向到登录页

5. [Frontend] 实现 Token 自动刷新机制（预计 1.5h）
   - 输入：Token 过期时间
   - 输出：自动刷新逻辑
   - 依赖：Frontend 任务 2, Backend 任务 3
   - 验收标准：Token 过期前自动刷新
```

### Tester 任务

```
1. [Tester] 编写登录 API 单元测试（预计 1.5h）
   - 输入：API 契约
   - 输出：测试用例文件
   - 依赖：Backend 任务 4 完成
   - 验收标准：覆盖成功、失败、锁定场景

2. [Tester] 编写密码哈希工具测试（预计 0.5h）
   - 输入：密码工具类
   - 输出：测试用例
   - 依赖：Backend 任务 2 完成
   - 验收标准：覆盖哈希、验证、边界情况

3. [Tester] 编写前端登录组件测试（预计 1h）
   - 输入：Login 组件
   - 输出：组件测试文件
   - 依赖：Frontend 任务 1 完成
   - 验收标准：覆盖表单验证、提交、错误展示

4. [Tester] 执行登录流程 E2E 测试（预计 1.5h）
   - 输入：完整登录流程
   - 输出：E2E 测试报告
   - 依赖：所有 Backend + Frontend 任务完成
   - 验收标准：覆盖正常登录、错误处理、锁定流程
```

### 任务依赖图

```
[Backend 1] ──┬── [Backend 3] ──┬── [Backend 4] ── [Backend 5]
              │                 │
[Backend 2] ──┘                 ├── [Backend 6]
                                │
              ┌─────────────────┘
              │
              └── [Sync: API_READY] ── [Frontend 3]

[Frontend 1] ─────────────────────┘
[Frontend 2] ── [Frontend 4]
           └── [Frontend 5]

[Tester 1] ←── Backend 完成
[Tester 2] ←── Backend 任务 2
[Tester 3] ←── Frontend 任务 1
[Tester 4] ←── 全部完成
```

---

Sync: SCHEMA_READY → 可开始 Backend 任务 1
Sync: API_READY    → 可开始 Frontend 任务 3
Verdict: PASS      → 可开始 Tester 任务
```

---

**下一步行动**：请确认技术栈选型（前端框架、后端框架、数据库），我将生成具体的代码骨架和配置文件。

---
*此文档由 Architect-Agent 自动生成*
