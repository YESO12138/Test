# 数据模型文档



## 2. SCHEMA (数据驱动)

### 用户表

```sql
CREATE TABLE users (
    id          BIGSERIAL PRIMARY KEY,
    email       VARCHAR(255) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    nickname    VARCHAR(100),
    avatar      VARCHAR(500),
    status      VARCHAR(20) DEFAULT 'active',  -- active, locked, pending
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 索引策略
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_status ON users(status);
```

### 字段约束

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `email` | VARCHAR(255) | UNIQUE, NOT NULL | 登录凭证，需格式验证 |
| `password_hash` | VARCHAR(255) | NOT NULL | bcrypt加密，成本因子≥10 |
| `status` | VARCHAR(20) | DEFAULT 'active' | 枚举值约束 |

---

   - 输入：Schema 设计文档
   - 输出：User Model + Migration 文件
   - 依赖：无
   - 验收：数据库表创建成功，索引生效

2. [Backend] 实现密码加密服务（预计 1h）
   - 输入：密码明文
   - 输出：bcrypt 哈希值
   - 依赖：无
   - 验收：哈希验证通过，成本因子≥10

3. [Backend] 实现 JWT Token 服务（预计 2h）
   - 输入：用户ID
   - 输出：access_token + refresh_token
   - 依赖：无
   - 验收：Token 生成/验证/刷新功能正常

4. [Backend] 实现用户注册 API（预计 2h）
   - 输入：注册表单数据
   - 输出：用户创建成功响应
   - 依赖：任务1, 2
   - 验收：POST /api/v1/auth/register 返回正确响应

5. [Backend] 实现用户登录 API（预计 2h）
   - 输入：邮箱 + 密码
   - 输出：Token 对 + 用户信息
   - 依赖：任务2, 3
   - 验收：POST /api/v1/auth/login 返回正确响应

6. [Backend] 实现认证中间件（预计 1.5h）
   - 输入：HTTP Request + Authorization Header
   - 输出：注入 user_id 到 Request 或返回 401
   - 依赖：任务3
   - 验收：受保护路由正确拦截未授权请求

7. [Backend] 实现 Token 刷新 API（预计 1h）
   - 输入：refresh_token
   - 输出：新的 access_token
   - 依赖：任务3
   - 验收：POST /api/v1/auth/refresh 返回新 Token

8. [Backend] 实现获取当前用户 API（预计 1h）
   - 输入：认证后的 Request
   - 输出：用户详细信息
   - 依赖：任务6
   - 验收：GET /api/v1/users/me 返回正确用户信息
```

### Frontend 任务

```
1. [Frontend] 封装认证 API 服务层（预计 1h）
   - 输入：API 契约文档
   - 输出：authService.js (register, login, refresh, logout)
   - 依赖：无
   - 验收：API 调用返回正确 Promise

2. [Frontend] 实现登录表单组件（预计 2h）
   - 输入：用户输入
   - 输出：表单验证结果 + 提交数据
   - 依赖：无
   - 验收：表单验证、错误提示、提交功能正常

3. [Frontend] 实现注册表单组件（预计 2h）
   - 输入：用户输入
   - 输出：表单验证结果 + 提交数据
   - 依赖：无
   - 验收：邮箱格式验证、密码强度提示、提交功能正常

4. [Frontend] 实现认证状态管理（预计 2h）
   - 输入：登录/登出动作
   - 输出：全局用户状态 + Token 存储
   - 依赖：任务1
   - 验收：登录后状态持久化，刷新页面状态保持

5. [Frontend] 实现路由守卫（预计 1h）
   - 输入：路由跳转
   - 输出：认证通过/重定向到登录页
   - 依赖：任务4
   - 验收：未认证用户访问受保护路由被拦截

6. [Frontend] 实现登录页面布局（预计 1h）
   - 输入：登录表单组件
   - 输出：完整登录页面
   - 依赖：任务2
   - 验收：页面布局符合设计规范

7. [Frontend] 实现注册页面布局（预计 1h）
   - 输入：注册表单组件
   - 输出：完整注册页面
   - 依赖：任务3
   - 验收：页面布局符合设计规范
```

### Tester 任务

```
1. [Tester] 编写注册 API 单元测试（预计 1h）
   - 输入：注册 API 端点
   - 输出：测试用例 + 覆盖率报告
   - 依赖：Backend 任务4完成
   - 验收：覆盖率 ≥ 80%

2. [Tester] 编写登录 API 单元测试（预计 1h）
   - 输入：登录 API 端点
   - 输出：测试用例 + 覆盖率报告
   - 依赖：Backend 任务5完成
   - 验收：覆盖率 ≥ 80%

3. [Tester] 编写认证中间件测试（预计 1h）
   - 输入：中间件函数
   - 输出：测试用例
   - 依赖：Backend 任务6完成
   - 验收：覆盖有效/无效/过期 Token 场景

4. [Tester] 编写端到端认证流程测试（预计 2h）
   - 输入：完整认证流程
   - 输出：E2E 测试报告
   - 依赖：Backend + Frontend 全部完成
   - 验收：注册→登录→访问受保护资源→登出 流程通过
```

### 任务依赖图

```
Backend Track:
[Task1] ──┬──> [Task4] ──┐
          │              │
[Task2] ──┴──> [Task5] ──┼──> [Task6] ──> [Task8]
          │              │       │
[Task3] ──┴──> [Task7] ──┘       │
                                │
Frontend Track:                  │
[Task1] ──> [Task4] ──> [Task5] │
[Task2] ──> [Task6]             │
[Task3] ──> [Task7]             │
                                │
Tester Track:                    │
[Task1] <───────────────────────┘
[Task2] <───────────────────────┘
[Task3] <───────────────────────┘
[Task4] <─────────────────────── 全部完成
```

---


---
*此文档由 Architect-Agent 自动生成*
