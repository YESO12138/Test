# 数据模型文档


## 实体列表
- **User**: 用户账户
- **Session**: 会话管理
- **LoginLog**: 审计日志
- `POST /api/v1/auth/login` - 登录
- `POST /api/v1/auth/refresh` - 刷新Token
- `POST /api/v1/auth/logout` - 登出


## 2. SCHEMA (数据驱动)

### 用户表

```sql
CREATE TABLE users (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    username        VARCHAR(50) NOT NULL UNIQUE,
    email           VARCHAR(255) NOT NULL UNIQUE,
    password_hash   VARCHAR(255) NOT NULL,
    status          VARCHAR(20) NOT NULL DEFAULT 'ACTIVE' 
                    CHECK (status IN ('ACTIVE', 'INACTIVE', 'LOCKED', 'PENDING')),
    login_attempts  INTEGER NOT NULL DEFAULT 0,
    locked_until    TIMESTAMP WITH TIME ZONE,
    last_login_at   TIMESTAMP WITH TIME ZONE,
    last_login_ip   VARCHAR(45),
    created_at      TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

-- 索引策略
CREATE INDEX idx_users_username ON users(username);
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_status ON users(status);
```

### 会话表

```sql
CREATE TABLE sessions (
    id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id        UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    token_hash     VARCHAR(255) NOT NULL UNIQUE,
    refresh_token  VARCHAR(255) UNIQUE,
    ip_address     VARCHAR(45),
    user_agent     TEXT,
    expires_at     TIMESTAMP WITH TIME ZONE NOT NULL,
    created_at     TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_sessions_user_id ON sessions(user_id);
CREATE INDEX idx_sessions_expires_at ON sessions(expires_at);
```

### 登录日志表

```sql
CREATE TABLE login_logs (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id     UUID REFERENCES users(id) ON DELETE SET NULL,
    username    VARCHAR(50) NOT NULL,
    ip_address  VARCHAR(45) NOT NULL,
    user_agent  TEXT,
    status      VARCHAR(20) NOT NULL CHECK (status IN ('SUCCESS', 'FAILED', 'LOCKED')),
    fail_reason VARCHAR(100),
    created_at  TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_login_logs_user_id ON login_logs(user_id);
CREATE INDEX idx_login_logs_created_at ON login_logs(created_at);
```

---

| BE-001 | 设计并创建用户、会话、登录日志表结构 | 1h | 无 | migration文件 |
| BE-002 | 实现密码哈希工具类 | 0.5h | 无 | PasswordHasher |
| BE-003 | 实现JWT Token生成与验证服务 | 1h | BE-002 | TokenService |
| BE-004 | 实现登录核心逻辑(凭证验证、失败计数、锁定机制) | 2h | BE-001, BE-002 | AuthService.login() |
| BE-005 | 实现Token刷新接口 | 1h | BE-003 | AuthService.refresh() |
| BE-006 | 实现登出接口(Token失效处理) | 0.5h | BE-003 | AuthService.logout() |
| BE-007 | 实现登录日志记录(异步) | 1h | BE-001 | LoginLogService |
| BE-008 | 添加登录接口限流中间件(防暴力破解) | 1h | 无 | RateLimitMiddleware |
| BE-009 | 编写登录模块单元测试 | 2h | BE-004~BE-006 | test覆盖率>80% |

### 前端任务

| 编号 | 任务描述 | 预估时间 | 依赖 | 输出物 |
|------|----------|----------|------|--------|
| FE-001 | 创建登录页面UI组件(表单、错误提示) | 2h | 无 | LoginPage.vue |
| FE-002 | 实现表单验证逻辑(前端校验) | 1h | FE-001 | formValidation.ts |
| FE-003 | 实现登录API调用与错误处理 | 1h | FE-002, API契约 | authApi.ts |
| FE-004 | 实现Token存储策略 | 0.5h | FE-003 | tokenStorage.ts |
| FE-005 | 实现用户状态管理 | 1h | FE-003, FE-004 | useAuthStore |
| FE-006 | 实现路由守卫(未登录跳转) | 1h | FE-005 | router/guards.ts |
| FE-007 | 实现Token自动刷新机制 | 1.5h | FE-004 | tokenRefresh.ts |
| FE-008 | 实现登录状态持久化(remember_me) | 0.5h | FE-004 | persistentAuth.ts |

### 测试任务

| 编号 | 任务描述 | 预估时间 | 依赖 | 输出物 |
|------|----------|----------|------|--------|
| QA-001 | 编写登录接口集成测试 | 1.5h | BE-004 | integration_test.go |
| QA-002 | 编写登录流程E2E测试 | 2h | FE-006, BE-004 | login.e2e.spec.ts |
| QA-003 | 安全测试(SQL注入、XSS、暴力破解) | 2h | 全部完成 | security_report.md |

---

Sync: SCHEMA_READY → 触发后端开发
Verdict: PASS → 触发测试审计
```

---

| 数据模型 | `docs/schema/` | `user_session.md` |

---

- 数据模型: `docs/schema/user_session.md`

---
*Architect-Agent 审核通过 | Sync: API_CONTRACT_READY*
```

---

**架构师裁决**: 以上分析已完成逻辑降维，契约已锁定，任务已原子化拆解。建议按依赖顺序执行，后端 BE-001~BE-003 可并行，前端需等待 API 契约确认后启动。

---
*此文档由 Architect-Agent 自动生成*
