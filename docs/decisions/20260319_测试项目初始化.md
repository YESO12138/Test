# 架构决策记录: 测试项目初始化

## 元信息
- **日期**: 2026-03-19
- **状态**: 已决策
- **决策者**: Architect-Agent

## 背景
测试空项目检测和初始化功能

## 建模分析

### 实体 (Entities)
- 待定义

### 状态 (States)
- `Sync: STRUCTURE_READY` → 触发配置文件生成
- `Verdict: INIT_COMPLETE` → 触发项目开发阶段
- 采用**结构化初始化**模式，确保项目骨架一致性
- 定义 4 个核心目录 + 2 个配置文件的最小可行结构
- 文件权限：前置检查
- 路径冲突：提供选项

### 边界 (Boundaries)
**后端职责**:
- 待定义

**前端职责**:
- 待定义

## 决策内容

# 项目初始化测试分析

## 1. MODELING (逻辑预演)

### 实体

| 实体 | 属性 | 说明 |
|------|------|------|
| **Project** | `name`, `path`, `status`, `structure` | 项目核心对象 |
| **Directory** | `path`, `type`, `required` | 目录结构单元 |
| **ConfigFile** | `name`, `template`, `required` | 配置文件定义 |

### 状态

```
[EMPTY] ──initialize()──> [INITIALIZED] ──validate()──> [VALID/INVALID]
```

| 状态 | 触发条件 | 后续动作 |
|------|----------|----------|
| EMPTY | 检测到空目录 | 执行初始化 |
| INITIALIZED | 初始化完成 | 执行验证 |
| VALID | 结构完整 | 可开始开发 |
| INVALID | 结构缺失 | 修复或重建 |

### 边界

| 责任域 | 职责 |
|--------|------|
| **后端** | 项目结构定义、初始化逻辑执行、文件系统操作、配置模板管理 |
| **前端** | 项目状态展示、初始化进度显示、错误信息提示、用户交互 |

---

## 2. SCHEMA (数据驱动)

本项目不涉及数据库，但需定义**项目结构 Schema**：

```
project-root/
├── docs/
│   ├── decisions/     # ADR 架构决策记录
│   ├── api/           # API 规格书
│   ├── schema/        # 数据模型定义
│   └── designs/       # 技术方案文档
├── src/               # 源代码
├── tests/             # 测试代码
├── config/            # 配置文件
├── README.md          # 项目说明
└── .gitignore         # Git 忽略规则
```

---

## 3. CONTRACT (契约锁定)

无外部 API，定义**内部模块契约**：

```typescript
interface ProjectInitializer {
  // 检测项目状态
  detectProject(path: string): Promise<ProjectStatus>;
  
  // 执行初始化
  initializeProject(path: string, options?: InitOptions): Promise<InitResult>;
  
  // 验证结构
  validateStructure(path: string): Promise<ValidationResult>;
}

interface ProjectStatus {
  isEmpty: boolean;
  existingFiles: string[];
  missingDirs: string[];
  missingFiles: string[];
}

interface InitResult {
  success: boolean;
  createdDirs: string[];
  createdFiles: string[];
  errors: InitError[];
}
```

---

## 4. ISSUE (原子拆解)

| # | 任务 | 粒度 | 输入 | 输出 | 依赖 |
|---|------|------|------|------|------|
| 1 | 空项目检测逻辑实现 | 1h | 项目路径 | `ProjectStatus` | 无 |
| 2 | 目录结构初始化 | 2h | 结构定义 | 创建的目录列表 | #1 |
| 3 | 配置文件模板生成 | 2h | 模板定义 | 生成的配置文件 | #2 |
| 4 | 初始化验证与报告 | 1h | 项目路径 | `ValidationResult` | #3 |

**同步信号**：
- `Sync: STRUCTURE_READY` → 触发配置文件生成
- `Verdict: INIT_COMPLETE` → 触发项目开发阶段

---

## 5. 风险评估

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| 文件系统权限不足 | 中 | 前置权限检查，友好错误提示 |
| 路径冲突（已存在文件） | 中 | 提供覆盖/跳过/合并选项 |
| 跨平台编码问题 | 低 | 强制 UTF-8 编码 |
| 模板渲染失败 | 低 | 模板预验证机制 |

---

## 6. 输出

### 完整分析报告

将沉淀为文档：`docs/decisions/20250112_project_initialization.md`

### 精简摘要（用于 Issue 评论）

```markdown
## 项目初始化测试 - 架构分析摘要

### 核心决策
- 采用**结构化初始化**模式，确保项目骨架一致性
- 定义 4 个核心目录 + 2 个配置文件的最小可行结构

### 任务拆解
| # | 任务 | 粒度 | 依赖 |
|---|------|------|------|
| 1 | 空项目检测 | 1h | - |
| 2 | 目录初始化 | 2h | #1 |
| 3 | 配置生成 | 2h | #2 |
| 4 | 验证报告 | 1h | #3 |

### 状态流
`[EMPTY] → [INITIALIZED] → [VALID/INVALID]`

### 风险
- 文件权限：前置检查
- 路径冲突：提供选项

### 文档沉淀
完整报告：`docs/decisions/20250112_project_initialization.md`

---
`Architect-Agent: PASS` | 同步信号：`INIT_TEST_READY`
```

---

**架构师裁决**：此测试任务逻辑清晰，边界明确，可进入执行阶段。

`Verdict: PASS` → 触发开发 Agent 执行初始化任务

## 后果
- 正面影响：待评估
- 负面影响：待评估
- 风险：参见风险评估章节

---
*此文档由 Architect-Agent 自动生成*
