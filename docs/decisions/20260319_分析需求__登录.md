# 架构决策记录: 分析需求: 登录

## 元信息
- **日期**: 2026-03-19
- **状态**: 已决策
- **决策者**: Architect-Agent

## 背景

请分析以下需求并分解为具体任务：

需求标题: 登录
需求描述: 实现登录
优先级: high

请按照架构师需求处理强制流进行分析：

## 1. MODELING (逻辑预演)
请明确建模三要素：
- **实体 (Entities)**：核心对象及其属性
- **状态 (States)**：业务流程中的状态变迁
- **边界 (Boundaries)**：后端与前端的责任划分

## 2. SCHEMA (数据驱动)
如果涉及数据变更，请定义数据模型。

## 3. CONTRACT (契约锁定)
如果涉及 API，请定义接口契约。

## 4. ISSUE (原子拆解)
请将需求拆解为具体任务，每个任务粒度 < 4 小时。
必须明确标注任务类型：[Backend]、[Frontend]、[Tester]

## 5. 风险评估
请指出潜在风险和替代方案。


## 建模分析

### 实体 (Entities)
- 待定义

### 状态 (States)
- 输入：Schema 设计文档
- 输出：User Model, LoginLog Model, Migration 文件
- 依赖：无
- 验收标准：迁移可执行，表结构正确
- 输入：密码明文
- 输出：bcrypt 哈希值
- 依赖：无
- 验收标准：哈希可验证，强度因子 >= 10
- 输入：用户 ID, 密钥配置
- 输出：access_token, refresh_token
- 依赖：任务 1
- 验收标准：Token 可签发、可验证、可刷新
- 输入：email, password
- 输出：用户信息 + Token 或错误响应
- 依赖：任务 1, 2, 3
- 验收标准：符合 API 契约，错误码正确
- 输入：失败次数阈值(3), 锁定时长(15min)
- 依赖：任务 4
- 验收标准：连续失败 3 次后锁定
- 输入：refresh_token
- 输出：成功响应
- 依赖：任务 3
- 验收标准：Token 失效
- 输入：登录事件
- 输出：日志记录
- 依赖：任务 1
- 验收标准：每次登录有记录
- 输入：设计稿/原型
- 输出：Login 组件
- 依赖：无
- 验收标准：包含邮箱、密码输入，表单验证
- 输入：用户数据结构
- 输出：Auth Store (user, token, isAuthenticated)
- 依赖：无
- 输入：API 契约
- 输出：login service, error handling
- 依赖：Frontend 任务 1, Backend 任务 4 (Sync: API_READY)
- 验收标准：调用成功后更新 Store
- 输入：路由配置
- 输出：Protected Route 组件
- 依赖：Frontend 任务 2
- 验收标准：未认证用户重定向到登录页
- 输入：Token 过期时间
- 输出：自动刷新逻辑
- 依赖：Frontend 任务 2, Backend 任务 3
- 验收标准：Token 过期前自动刷新
- 输入：API 契约
- 输出：测试用例文件
- 依赖：Backend 任务 4 完成
- 验收标准：覆盖成功、失败、锁定场景
- 输入：密码工具类
- 输出：测试用例
- 依赖：Backend 任务 2 完成

### 边界 (Boundaries)
**后端职责**:
- 依赖：所有 Backend + Frontend 任务完成

**前端职责**:
- 依赖：Frontend 任务 1 完成

## 决策内容

# 登录需求架构分析报告

> **Architect Verdict**: 需求描述过于模糊，但基于行业标准登录流程，我将进行完整的逻辑降维和任务拆解。

---

## 1. MODELING (逻辑预演)

### 实体

| 实体 | 核心属性 | 业务含义 |
|------|----------|----------|
| **User** | `id`, `email`, `password_hash`, `status`, `failed_attempts`, `locked_until` | 用户账户主体 |
| **Session** | `access_token`, `refresh_token`, `expires_at` | 认证会话 |
| **LoginLog** | `user_id`, `ip`, `user_agent`, `status`, `created_at` | 审计追踪 |

### 状态

```
用户账户状态流转：
[Active] ←→ [Locked] → [Active(解锁)]
    ↓
[Inactive(注销)]

登录流程状态：
[未认证] → [验证中] → [成功] → [已认证]
                ↓
             [失败] → [重试(3次内)] → [锁定(超3次)]
```

### 边界

| 层级 | 职责边界 |
|------|----------|
| **Backend** | 凭证验证、密码比对、Token 签发、登录日志、账户锁定策略 |
| **Frontend** | 表单 UI、本地验证、Token 存储、状态同步、路由守卫 |

---

## 2. SCHEMA (数据驱动)

### 用户表

```sql
CREATE TABLE users (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email           VARCHAR(255) UNIQUE NOT NULL,
  username        VARCHAR(50) UNIQUE,
  password_hash   VARCHAR(255) NOT NULL,
  status          VARCHAR(20) DEFAULT 'active' CHECK (status IN ('active', 'inactive', 'locked')),
  failed_attempts INTEGER DEFAULT 0,
  locked_until    TIMESTAMP,
  last_login_at   TIMESTAMP,
  created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_status ON users(status);
```

### 登录日志表

```sql
CREATE TABLE login_logs (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id      UUID REFERENCES users(id),
  ip_address   VARCHAR(45),
  user_agent   TEXT,
  status       VARCHAR(20) CHECK (status IN ('success', 'failed')),
  created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_login_logs_user ON login_logs(user_id, created_at DESC);
```

---

## 3. CONTRACT (契约锁定)

### API 端点定义

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/api/v1/auth/login` | 用户登录 |
| POST | `/api/v1/auth/logout` | 用户登出 |
| POST | `/api/v1/auth/refresh` | 刷新令牌 |

### 登录接口契约

**Request:**
```json
POST /api/v1/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "plaintext_password"
}
```

**Response (Success):**
```json
{
  "code": 0,
  "data": {
    "user": {
      "id": "uuid",
      "email": "user@example.com",
      "username": "johndoe"
    },
    "tokens": {
      "access_token": "eyJ...",
      "refresh_token": "eyJ...",
      "expires_in": 3600
    }
  }
}
```

**Response (Error):**
```json
{
  "code": 40101,
  "message": "Invalid credentials",
  "data": {
    "remaining_attempts": 2
  }
}
```

### 错误码规范

| 错误码 | 含义 | HTTP Status |
|--------|------|-------------|
| 40101 | 凭证无效 | 401 |
| 40102 | 账户已锁定 | 403 |
| 40103 | Token 过期 | 401 |
| 40001 | 参数验证失败 | 400 |

---

## 4. ISSUE (原子拆解)

### Backend 任务

```
1. [Backend] 创建用户数据模型与数据库迁移（预计 1.5h）
   - 输入：Schema 设计文档
   - 输出：User Model, LoginLog Model, Migration 文件
   - 依赖：无
   - 验收标准：迁移可执行，表结构正确

2. [Backend] 实现密码哈希工具类（预计 1h）
   - 输入：密码明文
   - 输出：bcrypt 哈希值
   - 依赖：无
   - 验收标准：哈希可验证，强度因子 >= 10

3. [Backend] 实现 JWT Token 服务（预计 2h）
   - 输入：用户 ID, 密钥配置
   - 输出：access_token, refresh_token
   - 依赖：任务 1
   - 验收标准：Token 可签发、可验证、可刷新

4. [Backend] 实现登录 API 核心逻辑（预计 2h）
   - 输入：email, password
   - 输出：用户信息 + Token 或错误响应
   - 依赖：任务 1, 2, 3
   - 验收标准：符合 API 契约，错误码正确

5. [Backend] 实现账户锁定策略（预计 1.5h）
   - 输入：失败次数阈值(3), 锁定时长(15min)
   - 输出：锁定状态更新
   - 依赖：任务 4
   - 验收标准：连续失败 3 次后锁定

6. [Backend] 实现登出 API（预计 0.5h）
   - 输入：refresh_token
   - 输出：成功响应
   - 依赖：任务 3
   - 验收标准：Token 失效

7. [Backend] 实现登录日志记录（预计 1h）
   - 输入：登录事件
   - 输出：日志记录
   - 依赖：任务 1
   - 验收标准：每次登录有记录
```

### Frontend 任务

```
1. [Frontend] 实现登录表单 UI 组件（预计 2h）
   - 输入：设计稿/原型
   - 输出：Login 组件
   - 依赖：无
   - 验收标准：包含邮箱、密码输入，表单验证

2. [Frontend] 实现认证状态管理 Store（预计 1.5h）
   - 输入：用户数据结构
   - 输出：Auth Store (user, token, isAuthenticated)
   - 依赖：无
   - 验收标准：状态可读写，持久化到 localStorage

3. [Frontend] 实现登录 API 调用与错误处理（预计 1.5h）
   - 输入：API 契约
   - 输出：login service, error handling
   - 依赖：Frontend 任务 1, Backend 任务 4 (Sync: API_READY)
   - 验收标准：调用成功后更新 Store

4. [Frontend] 实现路由守卫（预计 1h）
   - 输入：路由配置
   - 输出：Protected Route 组件
   - 依赖：Frontend 任务 2
   - 验收标准：未认证用户重定向到登录页

5. [Frontend] 实现 Token 自动刷新机制（预计 1.5h）
   - 输入：Token 过期时间
   - 输出：自动刷新逻辑
   - 依赖：Frontend 任务 2, Backend 任务 3
   - 验收标准：Token 过期前自动刷新
```

### Tester 任务

```
1. [Tester] 编写登录 API 单元测试（预计 1.5h）
   - 输入：API 契约
   - 输出：测试用例文件
   - 依赖：Backend 任务 4 完成
   - 验收标准：覆盖成功、失败、锁定场景

2. [Tester] 编写密码哈希工具测试（预计 0.5h）
   - 输入：密码工具类
   - 输出：测试用例
   - 依赖：Backend 任务 2 完成
   - 验收标准：覆盖哈希、验证、边界情况

3. [Tester] 编写前端登录组件测试（预计 1h）
   - 输入：Login 组件
   - 输出：组件测试文件
   - 依赖：Frontend 任务 1 完成
   - 验收标准：覆盖表单验证、提交、错误展示

4. [Tester] 执行登录流程 E2E 测试（预计 1.5h）
   - 输入：完整登录流程
   - 输出：E2E 测试报告
   - 依赖：所有 Backend + Frontend 任务完成
   - 验收标准：覆盖正常登录、错误处理、锁定流程
```

### 任务依赖图

```
[Backend 1] ──┬── [Backend 3] ──┬── [Backend 4] ── [Backend 5]
              │                 │
[Backend 2] ──┘                 ├── [Backend 6]
                                │
              ┌─────────────────┘
              │
              └── [Sync: API_READY] ── [Frontend 3]

[Frontend 1] ─────────────────────┘
[Frontend 2] ── [Frontend 4]
           └── [Frontend 5]

[Tester 1] ←── Backend 完成
[Tester 2] ←── Backend 任务 2
[Tester 3] ←── Frontend 任务 1
[Tester 4] ←── 全部完成
```

---

## 5. 风险评估

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| **暴力破解攻击** | 🔴 高 | 实现账户锁定策略 + 登录频率限制 (Rate Limiting) |
| **Token 泄露** | 🟡 中 | 使用 HttpOnly Cookie 存储 refresh_token，短期 access_token |
| **XSS 攻击** | 🟡 中 | 前端输入过滤，后端输出编码，CSP 策略 |
| **明文密码传输** | 🟡 中 | 强制 HTTPS，考虑前端加密传输 |
| **并发登录** | 🟢 低 | 可选：单点登录限制，多设备管理 |

### 替代方案

| 场景 | 方案 A (推荐) | 方案 B |
|------|---------------|--------|
| Token 方案 | JWT 无状态 | Session + Redis 有状态 |
| 密码存储 | bcrypt | argon2 (更强) |
| 前端状态 | Pinia/Vuex | React Query/SWR |

---

## 同步信号

```
Sync: SCHEMA_READY → 可开始 Backend 任务 1
Sync: API_READY    → 可开始 Frontend 任务 3
Verdict: PASS      → 可开始 Tester 任务
```

---

**下一步行动**：请确认技术栈选型（前端框架、后端框架、数据库），我将生成具体的代码骨架和配置文件。

## 后果
- 正面影响：待评估
- 负面影响：待评估
- 风险：参见风险评估章节

---
*此文档由 Architect-Agent 自动生成*
